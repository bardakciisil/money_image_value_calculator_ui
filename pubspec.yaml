name: money_image_value_calculator_ui
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.4.1 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0
  cupertino_icons: ^1.0.8
  image_picker: ^1.1.2
  dio: ^5.6.0
  dio_http: ^5.0.4
  get_it: ^7.2.0

flutter:
  uses-material-design: true
